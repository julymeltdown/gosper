// Runtime configuration
// This file can be replaced at deployment time
window.GOSPER_CONFIG = {
  // Backend URL - can be set via environment variable at build/deploy time
  // Leave empty to use same origin (for Ingress/reverse proxy setups)
  BACKEND_URL: ''
};
