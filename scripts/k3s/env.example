# Copy to scripts/k3s/.env and customize
export NAMESPACE=handy
export DOMAIN=handy.local

# Image registry and tags
export BE_IMAGE=ghcr.io/youruser/gosper-server:latest
export FE_IMAGE=ghcr.io/youruser/gosper-fe:latest

# Optional: registry login
# export REGISTRY_USER=
# export REGISTRY_PASS=

